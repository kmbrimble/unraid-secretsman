**SecretsMan**

Central secret storage for Unraid Docker templates, so a password or API key typed into the Docker GUI doesn't end up in plaintext in the template XML, in the docker run command the GUI shows back to you, and in every copy or backup of them. Includes encrypted backup and restore of the store.
